<script type="text/javascript" src="http://beta.openbadges.org/issuer.js"></script>

<script type="text/javascript">
function issue_badge(url)
{
	OpenBadges.issue([url], function(errors, successes) {alert(JSON.stringify(errors)); alert(JSON.stringify(successes));});
}
</script>

<ul>
<% @assertions.each do |assertion| %>
	<li>
		<%= assertion.badge.name %> | <%= assertion.user.email %> |
		<a href="#" onClick="issue_badge('<%=baking_url assertion%>')">Issue</a>
	</li>
<% end %>
</ul>